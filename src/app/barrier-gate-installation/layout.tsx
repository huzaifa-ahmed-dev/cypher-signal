import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Commercial Barrier Gates UAE',
  description: 'Heavy-duty barrier gate systems for parking lots and high-security access control in the UAE.',
};

export default function BarrierGateLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
