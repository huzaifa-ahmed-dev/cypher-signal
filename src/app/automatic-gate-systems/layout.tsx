import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Automatic Gate Systems Dubai',
  description: 'Premium sliding and swing automatic gates for residential and commercial properties in Dubai.',
};

export default function AutomaticGatesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
